generated: '2026-09-10'
method: searched
source: npm, PyPI, NuGet, RubyGems, Maven Central, Packagist, crates.io registry searches + https://github.com/orionadvisor
note: No first-party client libraries are published to any public package registry
  (npm search for orionadvisor returned nothing; no @orionadvisor scope exists).
  Orion's GitHub org ships a C# sample project demonstrating API calls, not a
  distributable SDK, so no SDKs pointer is emitted.
packages:
- language: csharp
  registry: github
  name: WebApiSdkSample
  url: https://github.com/orionadvisor/WebApiSdkSample
  install: git clone https://github.com/orionadvisor/WebApiSdkSample
  official: true
  kind: sample
  version: null
  published: '2022-03-01'
  note: '.NET sample project demonstrating calls to the Orion Web API and linking
    to Orion Connect UI. Not published to a registry; version read as null because
    no release/tag exists — last repo update 2022-03-01 (GitHub API).'
